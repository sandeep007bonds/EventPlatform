<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Identity.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../Identity.Domain/Identity.Domain.csproj" />
    <ProjectReference Include="../../Identity.Application/Identity.Application.csproj" />
    <ProjectReference Include="../../Identity.Infrastructure/Identity.Infrastructure.csproj" />
    <!-- Referenced only for the JWKS DTOs (JsonWebKeyDto/JsonWebKeySetDto) — the round-trip test
         proves the exact JSON those DTOs produce is parseable by the same library ASP.NET Core's
         own discovery machinery uses. -->
    <ProjectReference Include="../../Identity.Api/Identity.Api.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Shouldly" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="Testcontainers.PostgreSql" />
    <PackageReference Include="NetArchTest.Rules" />
  </ItemGroup>

</Project>
