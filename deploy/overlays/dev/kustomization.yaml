apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - keyvault-secretproviderclass.yaml
  - ingress.yaml
  - ../../base

namespace: eventplatform-dev

# Merges into the gateway Deployment from base rather than editing it there: the allowed browser
# origin is environment-specific, the way the Ingress host is.
patches:
  - path: gateway-cors-patch.yaml

# `labels` replaces the deprecated `commonLabels`. `includeSelectors: true` is what makes this a
# pure rename with no behaviour change — it is what commonLabels did, and it matters more than it
# looks: a Deployment's selector is immutable once created, so flipping this to the `labels`
# default of false against a cluster that already has these workloads would make every Deployment
# fail to update rather than quietly drift.
labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/environment: dev

# Non-secret shared config, mounted via envFrom in every backend service's
# Deployment (see deploy/base/*/deployment.yaml). The gateway sets its own
# ASPNETCORE_ENVIRONMENT directly instead — see deploy/base/gateway/deployment.yaml
# for why it can't share this value.
configMapGenerator:
  - name: eventplatform-config
    literals:
      - ASPNETCORE_ENVIRONMENT=Development
      # Where AddDefaultObservability's OTLP exporter sends traces and metrics. Unset until now,
      # which meant the services exported to nowhere in the cluster. gRPC (4317) is the .NET
      # exporter's default protocol; the collector also listens on 4318 for HTTP.
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      # Real token validation (ADR-0032). With no Jwt:DevSigningKey set, AddEventPlatformAuthentication
      # takes its Authority branch: ASP.NET Core fetches {Authority}/.well-known/openid-configuration,
      # caches Identity's JWKS, and validates RS256 signatures against it. The issuer it expects comes
      # from that same document, which is why Identity__Jwt__Issuer must be this exact URL.
      - Jwt__Authority=http://identity
      # In-cluster Service DNS is plain HTTP; the traffic never leaves the cluster. Leaving this true
      # makes discovery fail with a metadata-address error that reads like a wrong URL.
      - Jwt__RequireHttpsMetadata=false

# CI overwrites every entry here on each push (`kustomize edit set image
# <placeholder>=<acr-login-server>/<service>:<tag>`) — the values below are
# just placeholders for the first-ever commit, before CI has run once.
images:
  - name: catalog-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/catalog
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: inventory-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/inventory
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: ordering-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/ordering
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: payments-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/payments
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: ticketing-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/ticketing
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: communication-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/communication
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: media-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/media
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: identity-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/identity
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: queue-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/queue
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: venue-placeholder
    newName: REPLACE_ME.azurecr.io/venue
    newTag: REPLACE_ME
  - name: gateway-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/gateway
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
  - name: frontend-placeholder
    newName: acreventplatformdev3j13xu.azurecr.io/frontend
    newTag: d15e9484e66f04e82564e8950ad4ca0a91f93338
