apiVersion: v1
kind: Service
metadata:
  name: communication
  labels:
    app.kubernetes.io/name: communication
    app.kubernetes.io/part-of: eventplatform
spec:
  selector:
    app.kubernetes.io/name: communication
  ports:
    - name: http
      port: 80
      targetPort: 8080
