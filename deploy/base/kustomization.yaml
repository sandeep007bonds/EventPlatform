apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Dapr components first: the services' sidecars resolve them by name at startup.
  - dapr-components
  - catalog
  - inventory
  - ordering
  - payments
  - ticketing
  - communication
  - media
  - identity
  - queue
  - gateway
