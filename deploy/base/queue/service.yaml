apiVersion: v1
kind: Service
metadata:
  name: queue
  labels:
    app.kubernetes.io/name: queue
    app.kubernetes.io/part-of: eventplatform
spec:
  selector:
    app.kubernetes.io/name: queue
  ports:
    - name: http
      port: 80
      targetPort: 8080
