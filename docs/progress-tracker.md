# Progress & Tech-Debt Tracker

Single place to track what's done, what's in progress, and — importantly — the
**deferred tasks and tech debt**, so nothing is lost. Update this with each
meaningful change.

Cloud work is **no longer deferred** — the platform now has Terraform for a full
`dev` Azure topology, GitOps via Argo CD, ingress + TLS, an OTel collector, and a
one-command provisioning script. See the cloud table below for what actually
shipped versus what is genuinely still missing.

**Legend:** ✅ done · 🚧 in progress · ⏸️ deferred (on hold) · ⬜ not started

---

## Progress

### Foundations & documentation
- ✅ Architecture, requirements, feature flows (6)
- ✅ ADRs 0001–0014
- ✅ HLD, DFD, LLD (Phase 1 seated)
- ✅ Engineering guidelines + golden rules
- ✅ Build config: `.editorconfig`, `Directory.Build.props`, CPM, PR template
- ✅ Root + per-service `CLAUDE.md`
- ✅ Roadmap issues (#1–#11) on GitHub
- ✅ **System reference** (`docs/system-reference.md`): the single as-built description of the platform — capabilities by persona, buyer/organizer/gate-staff journeys, the checkout saga and no-oversell paths end to end, the ownership/API/event table for all nine services, cross-cutting mechanisms (outbox, idempotency, tenancy, auth), and an index into the other ~40 docs. Supersedes `data-flow-and-service-boundaries.md`, which was deleted: it documented **seven** services (no `queue`, no `media`) and predated the async-payment rewrite, so it had become actively misleading

### Code scaffold
- ✅ Solution (`.slnx`)
- ✅ `EventPlatform.Hosting` — shared service defaults (auth, OpenAPI, JSON, OTel, health)
- ✅ `EventPlatform.Contracts` — base integration event + sample
- ✅ Local Docker dev stack (compose + Dapr components + guide)
- ✅ **One-click local dev:** `./scripts/dev-up.sh` starts Postgres/Redis/Jaeger, waits for health, applies every schema (`scripts/db-migrate.sh`), then starts the services + Dapr sidecars via a Dapr multi-app run template (`platform/dapr/dapr.yaml`) — replaces a terminal per service with one command (Ctrl+C stops everything). Media.Api and the gateway start alongside as plain processes; neither uses Dapr. `scripts/dev-down.sh` tears down Docker; `scripts/dev-token.sh` mints the dev auth JWT. Docs (`local-development.md`, `local-e2e-walkthrough.md`) updated to lead with it
- ✅ **Build hardening — green under warnings-as-errors** (deps patched, .NET 10 pruning, global usings, analyzer config)
- ✅ **Service structure flattened** (no `src/`) — layers directly under `services/<name>/`; standard for all services
- 🚧 **Catalog service implementation (issue #6)** ← current focus
  - ✅ Domain (`Event` aggregate, `EventStatus`)
  - ✅ Application slices: `CreateEvent`, `GetEvent` (+ FluentValidation pipeline)
  - ✅ Infrastructure: EF Core + Postgres (`CatalogDbContext`, repository)
  - ✅ API wired: `POST /v1/events`, `GET /v1/events/{id}` via MediatR
  - ✅ Local `dotnet build` green; runs against local Postgres (`EnsureCreated`)
  - ✅ `PublishEvent` slice (draft → published)
  - ✅ **Transactional outbox + Dapr (first use):** `PublishEvent` enqueues `EventPublished` into the `outbox` table in the same transaction; shared `EventPlatform.Messaging` relay publishes it to Dapr pub/sub (at-least-once, CloudEvent id = outbox id)
  - ✅ **Seat map:** `SeatMap`/`Seat` domain, `DefineSeatMap` + `GetSeatMap` slices, `POST`/`GET /v1/events/{id}/seatmap`. Publish now requires a seat map and stamps `SeatCount` on `EventPublished`; `GetSeatMap` is the hand-off Inventory reads ← **verify build locally**
  - ~~Local dev schema: `Database.EnsureCreatedAsync()` on startup~~ — **superseded by ADR-0029.** No service does this any more; `dev-up.sh` applies real migrations via `scripts/db-migrate.sh` before anything starts serving
  - ✅ Real EF Core migrations `InitialCreate` (Design ref + design-time factory already in place) — committed and applied via the shared `--migrate` entry point (ADR-0029)
- 🚧 **Inventory & Hold — no-oversell core (issue #7)**
  - ✅ **Stage A — provisioning:** service scaffold (Domain `InventoryItem`/`Hold`/`LedgerEntry`, lean Application, EF + Postgres, outbox, migration scaffolding); consumes `EventPublished` via Dapr pub/sub and generates inventory by pulling Catalog's seat map (Dapr service invocation); `GET /v1/events/{id}/inventory` ← **verify build locally**
  - ✅ **Stage B — hold hot path:** Redis Lua atomic check-and-set (`RedisHoldStore`) as the fast gate + Postgres optimistic concurrency (`Version`) as the final authority + outbox; `POST /v1/holds` and `DELETE /v1/holds/{id}` emit `SeatHeld`/`SeatReleased`. Sparse Redis model (missing key = available; no per-seat seeding) ← **verify build locally**
  - ✅ **Stage C — expiry reaper:** `ExpiredHoldReaper` background service reclaims holds past `expires_at` — returns seats to available in Postgres (authority), clears the Redis seat keys, emits `SeatReleased`. Each hold is its own unit of work ← **verify build locally**
  - ✅ **Saga hooks (for #8):** `GET /v1/holds/{id}` (validate: owner/expiry + priced lines), internal `POST /v1/holds/{id}/convert` (idempotent convert-to-sold: seats → `S`, emits `SeatSold`)
  - ✅ Redis↔Postgres drift reconciler: `InventoryReconciler` background service detects a flushed/restarted Redis via a sentinel key and rebuilds the fast gate from Postgres (held seats with remaining TTL + sold + blocked seats), joining held items back to their active hold for the TTL. Only re-applies restrictions, never frees a seat (cannot cause oversell); runs on startup + interval — verified by CI
  - ✅ **Organizer seat blocking:** `InventoryItem.Block()`/`Unblock()` (Available ↔ Blocked, the previously-unused `Blocked` status now wired up); `SeatBlockingService` (all-or-nothing across requested seats, Postgres-first then Redis, same authority order as release/convert); `POST /v1/events/{id}/inventory/block` and `/unblock`; emits `SeatBlocked`/`SeatUnblocked`; reconciler restores blocked seats on a Redis rebuild — verified by CI
  - ✅ Real EF Core migrations `InitialCreate` (inventory_item, hold, hold_item, inventory_ledger, outbox) — committed and applied via the shared `--migrate` entry point (ADR-0029)
- 🚧 **Ordering — checkout saga (#8)**
  - ✅ Service scaffold: `Order`/`OrderLine` domain, `CheckoutService` saga (validate → create → charge → convert → confirm + compensation), EF + Postgres, outbox, migration scaffolding; `POST /v1/checkout` (Idempotency-Key), `GET /v1/orders/{id}` ← **verify build locally**
  - ✅ Inventory hooks reused: `IHoldClient` (Dapr) → GET hold / convert / release; payment **stubbed** (`StubPaymentClient`)
  - ✅ **Durability — Dapr Workflow:** `CheckoutWorkflow` (deterministic orchestrator) + 8 activities (fetch hold, create order, charge, convert, confirm + compensations release/refund/fail). `/v1/checkout` schedules the workflow and awaits completion; a crash mid-flight resumes where it left off (ADR-0010). Replaced the in-process `CheckoutService` — verified by CI
  - ✅ Real EF Core migrations `InitialCreate` (orders, order_line, outbox) — committed and applied via the shared `--migrate` entry point (ADR-0029)
  - ✅ Concurrent-duplicate checkout: `CreateOrderActivity` re-checks + `IOrderRepository.TryAddAsync` swallows the `(tenant, idempotency_key)` unique-violation (Postgres 23505); the losing racer re-fetches the winner and the workflow short-circuits to `Duplicate` (409) — no 500, no double charge — verified by CI
- 🚧 **Payments (#9)**
  - ✅ Service scaffold: `Payment` domain, `PaymentService` (idempotent charge/refund on `(order, key)`), EF + Postgres, outbox, migration scaffolding; internal `POST /v1/payments/charge` + `/refund`; emits `PaymentCaptured`/`PaymentFailed`/`PaymentRefunded`
  - ✅ Gateway behind `IPaymentGateway`; dev `SimulatedPaymentGateway`. **Ordering calls Payments** (`DaprPaymentClient`).
  - ✅ Concurrent-duplicate charge: `IPaymentRepository.TrySaveChangesAsync` swallows the `(order, idempotency_key)` unique-violation (Postgres 23505); the losing racer re-fetches the winner (PSP dedupes the charge on the same key, loser's outbox events roll back) — no 500, no double charge — verified by CI
  - ✅ **Stripe gateway (Stripe.net):** `StripePaymentGateway.CreateIntentAsync` creates (never
    confirms) a PaymentIntent with `AutomaticPaymentMethods.Enabled = true` (Stripe idempotency
    key), refunds via the Refund API. Selected automatically when `Payments:Stripe:SecretKey` is
    configured (Key Vault / user-secrets / env) — **never committed**. PCI SAQ-A.
  - ✅ **Stripe webhook inbox (async capture / 3DS / refunds):** `POST /v1/payments/webhooks/stripe` verifies the `Stripe-Signature` against `Payments:Stripe:WebhookSecret` (`StripeWebhookGateway`), reconciles the `Payment` idempotently (`PaymentWebhookService`, new idempotent domain transitions), and emits the matching outbox event. At-least-once → exactly-once via a `processed_webhook_event` dedupe ledger committed in the same transaction; neutral `PaymentWebhookNotification` keeps the Stripe SDK out of Application/Domain — verified by CI
  - ✅ **T-stripe (ADR-0028): full async Payment Element — cards + UPI + 3-D Secure, no longer
    just plumbing.** Checkout is now genuinely asynchronous: `IPaymentGateway.CreateIntentAsync`
    takes no payment-method id at all — the buyer attaches and authenticates a payment method
    (card 3DS challenge, UPI app-switch, etc.) entirely client-side via Stripe's `PaymentElement`
    against the returned `ClientSecret`. `CheckoutWorkflow` creates the intent, extends the hold
    (`HoldOptions.PaymentExtensionTtl`, 15 min), then races
    `WaitForExternalEventAsync<PaymentOutcomeSignal>` against a timer — a new
    `PaymentCaptured`/`PaymentFailed` webhook subscriber in Ordering resumes the saga
    (`OrderId` doubles as the Dapr Workflow instance id, so no lookup table is needed).
    `OrderingEndpoints.CheckoutAsync` races a fast poll of the `Order` row against the full
    blocking wait so the buyer sees Payment Element promptly instead of waiting on saga
    completion. Frontend: `CheckoutPage.tsx` creates the intent before ever showing a payment
    form; `CheckoutPaymentForm.tsx` calls `stripe.confirmPayment({ redirect: 'if_required' })`;
    a new `CheckoutReturnPage.tsx` (`/checkout/:holdId/return`) handles the methods that redirect
    out and back. Local/CI checkout with no Stripe key configured still resolves instantly (the
    simulated gateway self-confirms, same saga path as real Stripe).
  - ✅ Real EF Core migrations `InitialCreate` (payment, outbox, **processed_webhook_event**) — committed and applied via the shared `--migrate` entry point (ADR-0029)
- ✅ **Ticketing (#10):** `Ticket` domain, `TicketIssuingService` (idempotent, one ticket per sold seat, CSPRNG scan token), EF + Postgres, outbox, migration scaffolding; consumes `OrderConfirmed` via Dapr pub/sub, emits `TicketIssued`; `GET /v1/orders/{id}/tickets`, `GET /v1/tickets/{id}` — verified by CI
  - ✅ Real EF Core migrations `InitialCreate` (ticket, outbox) — committed and applied via the shared `--migrate` entry point (ADR-0029)
  - ⬜ T-ticket-token: sign/rotate the scan token before production
- ✅ **Communication (ADR-0016, notifications scope):** sixth service — `DeliveryLogEntry`/`NotificationTemplate` domain, dual-vendor (Azure Communication Services + Twilio) `IEmailSender`/`ISmsSender`/`IWhatsAppSender` ports, config-gated per channel with a dev/logging fallback (same pattern as Payments' Stripe gate); single `POST /v1/notifications/send` endpoint (internal, all three channels, email template-driven via embedded Scriban templates); subscribes to `OrderConfirmed`/`TicketIssued` for redelivery-safety (`ProcessedNotificationEvent` dedup ledger) but real delivery is deferred — no Identity/user-profile service exists yet to resolve a recipient from the bare `UserId` either event carries (`IRecipientResolver`/`UnavailableRecipientResolver`). No outbox (never publishes). First service in the repo with a `tests/` project; CI gained its first `dotnet test` step
  - ✅ Real EF Core migrations `InitialCreate` (delivery_log, processed_notification_event) — committed and applied via the shared `--migrate` entry point (ADR-0029)
  - ⬜ Real `IRecipientResolver` implementation once Identity (or any user-profile source) exists — unblocks actual Email/SMS/WhatsApp delivery for `OrderConfirmed`/`TicketIssued`
- 🚧 **No-oversell load test (#11)**
  - ✅ k6 harness (`platform/loadtest/`): `no-oversell.js` (N users race for **one** seat; hard gate `holds_succeeded: count<2`), `throughput.js` (sustained load over a big seat map; p95<250ms / p99<500ms / err<1%), `lib/jwt.js` (HS256 dev-token minting), README
  - ✅ Dev-auth path in `EventPlatform.Hosting/AuthenticationExtensions.cs`: symmetric-key JWT validation gated on `Jwt:DevSigningKey` (Development-only; prod stays OIDC) — verified by CI
  - ⬜ Run against the live stack (needs Catalog + Inventory + Dapr + Postgres + Redis up locally) and record the numbers
- ✅ **Event tours, contact/social, enforced booking cutoff, and Reserved-vs-General-Admission tickets (ADR-0019, ADR-0020):** `Venue` removed; `EventGroup` (tour) added with its own `StartsAt`/`EndsAt` and contact/social defaults (per-leg override); `Event.EndsAt` required at creation; `OffSaleAt` renamed to the now-enforced `BookingEndsAt`. `SeatMap` sections can be `Reserved` or `GeneralAdmission` (mixed in one map); Inventory gained a counter-based `GeneralAdmissionAllocation` (fail-closed Redis capacity gate, mirroring but distinct from the fail-open sparse seat model) and `EventInventorySettings` (enforced cutoff, checked first in `PlaceHoldAsync`). `OrderLine`/`Ticket`/`LedgerEntry`/`OrderConfirmed`/`TicketIssued` all widened with nullable seat-vs-GA fields rather than split into separate types. Frontend: tour/event forms, seat-map allocation-type toggle, GA quantity steppers alongside the reserved-seat grid, contact/social display and editing (shared `SocialLinksEditor`) — verified via `tsc -b`/`eslint`/`prettier`/`vite build`, all clean.
  - ⬜ No Inventory endpoint yet exposes a GA allocation's live remaining capacity to the buyer UI (the quantity stepper is capped by total section capacity; the server is still the real enforcer at hold time)
  - ⬜ No UI yet to edit an already-created tour's dates/contact/social after initial creation (the backend command/endpoint exist and are reachable via API)
  - ⬜ `InventoryReconciler` not extended to rebuild GA capacity counters after a Redis flush (fast-path availability only — Postgres remains authoritative, see ADR-0020)
  - ⬜ `BookingEndsAt` not editable post-publish (would need a new command + wiring the zero-consumer `EventUpdated` event into Inventory)
- ✅ **Ticket delivery email, check-in/scan, and per-buyer ticket limits (ADR-0021):** `CheckoutRequest.BuyerEmail` (required) threaded through `Order`/`OrderConfirmed`; new `OrderTicketsIssued`/`IssuedTicketSummary` contract published once per order after all its tickets are minted; Communication's `IntegrationEventNotificationHandler` gains a direct-send handler path (bypasses `NotificationSendService`/`IRecipientResolver`, one `SaveChangesAsync` for the delivery log + processed marker) rendering a new `order-tickets` Scriban template and sending via the dev/logging `IEmailSender`. `Ticket.CheckedInAt` + `ITicketRepository.GetByTokenAsync` + `POST /v1/tickets/scan` (new admin `ScanTicketPage` at `/admin/scan`). `Event.MaxTicketsPerBuyer` propagated to Inventory via `EventPublished`/`EventInventorySettings`; new `IInventoryRepository.GetBuyerCommittedQuantityAsync` sums a buyer's Active+Converted holds; `PlaceHoldOutcome.BuyerLimitExceeded` (409) enforced in `HoldService.PlaceHoldAsync` — verified via `tsc -b`/`eslint`/`prettier`/`vite build`, all clean.
  - ⬜ Archive/purge for consumed tickets — design captured as P3 above, nothing built
  - ⬜ Ticketing still has no `tests/` project (pre-existing gap, matches Catalog; Ordering now has one)
  - ⬜ No QR code image generation — the email and scan flow use the raw opaque token text only
- ✅ **`Ordering.Tests` — checkout-saga coverage, prompted by a real stall:** the async-payment
  rewrite (ADR-0028) shipped with `await CancellationTokenSource.CancelAsync()` inside
  `CheckoutWorkflow`, added only to satisfy analyzer S6966. An orchestrator may only await
  *durable* tasks, so the workflow engine ended the turn having scheduled nothing
  (`Sending 0 action(s)`) — captured payments sat in `AwaitingPayment` indefinitely, with a clean
  build, zero warnings, and no exception anywhere. Ordering had no test project at all. It now has
  three groups: `CheckoutWorkflowTests` (decision logic through a substituted `WorkflowContext` —
  outcomes, compensation ordering, both the push and pull capture paths), `LayeringTests`
  (NetArchTest, incl. `Ordering.Workflow` must not reference Infrastructure), and
  `OrchestratorPurityTests`, which scans orchestrator *sources* for replay-breaking constructs —
  mock-based tests structurally cannot catch that class of bug, since a substituted context has no
  executor counting actions.
  - ⬜ No end-to-end saga test against a real Dapr sidecar — the purity scan is a proxy for replay
    semantics, not a substitute for exercising them
- ✅ **Tenant-derivation fix for buyer actions (ADR-0022, build-order step 7):** `HoldService.PlaceHoldAsync`/`CheckoutWorkflow` now derive tenant from the resource (`EventInventorySettings.TenantId`/`Hold.TenantId`) instead of requiring the caller's `tenant_id` JWT claim — a precondition for real buyer tokens (which won't carry that claim) to work. Checkout idempotency re-scoped from `(tenant_id, idempotency_key)` to `(user_id, idempotency_key)`. Organizer-facing paths (seat block/unblock, `forTenant=true` order listing) unchanged. `scripts/dev-token.sh` gained a `TENANT_ID=""` opt-out for local testing.
- ✅ **Identity service (ADR-0016, buyer-identity half; build-order step 8):** seventh backend service — buyer phone+OTP authentication, and a lightweight OIDC token issuer so the 5 existing services validate its tokens through their existing `AuthenticationExtensions.cs` `Authority`-based JWT validation with **zero code changes**. `PhoneVerification` (keyed-HMAC-SHA256 OTP hashing, 5-minute TTL, 5-attempt lockout, 60s resend cooldown) and `BuyerAccount` (durable identity keyed by phone, created on first verification) domain; `POST /v1/otp/request`/`verify` (gateway-routed); hand-rolled `GET /.well-known/openid-configuration` + `GET /.well-known/jwks.json` (not gateway-routed — fetched server-to-server); RSA signing key generated once and persisted in Postgres (`SigningKey`), config-gated for a future Key Vault provider that is **not implemented yet** (fails fast, doesn't silently fall back). Tokens carry `sub`/`iss`/`aud`/`role: "buyer"` — deliberately **no `tenant_id`** (ADR-0022). OTP delivery reuses Communication's existing `POST /v1/notifications/send` via Dapr invocation — no changes needed there. Access-token-only, ~7-day lifetime — no refresh tokens (explicit decision).
  - ⬜ Key Vault-backed signing-key provider — gated (`Identity:Jwt:SigningKeySource=KeyVault`) but not implemented
  - ✅ Real EF Core migrations `InitialCreate` (phone_verifications, buyer_accounts, signing_keys) — landed with the repo-wide migrations work
  - ✅ Local-dev limitation resolved (ADR-0032): no service sets `Jwt:DevSigningKey` any more. Every service — locally and in-cluster — validates real RS256 tokens via OIDC discovery against Identity (`Jwt:Authority` = `http://localhost:5087` locally, `http://identity` in the cluster). The dev HS256 branch still exists in `AuthenticationExtensions.cs` and `jwt-dev-signing-key` is still synced from Key Vault, deliberately unused — an escape hatch, not the default path
- ✅ **Frontend: buyer `AuthContext` swapped to real Identity OTP login (build-order step 10):** `EventsListPage`/`EventDetailPage` stay anonymous; `/events/:id/seats` is no longer behind `ProtectedRoute` — a buyer picks seats/GA quantities freely. The identity gate moved to the "Hold selection" action itself: `SeatSelectionPage.handleHold` opens an `OtpLoginFlow` inside a `Modal` when `user` is null, and a successful verification (`AuthContext.loginWithOtp` → `POST /v1/otp/verify`) mints the session on the spot and auto-continues the hold — no second click, no redirect. New shared `OtpLoginFlow` component (phone → code, Ant `Input.OTP`) is reused bare on `/login` (new `BuyerLoginPage`, replacing the old dev-login form for buyers) and inside the seat-page modal. `StoredUser.email`/`.tenantId` widened to optional to accommodate OTP buyers, who have neither.
- ✅ **Organizer auth: in-house email+password via Identity, superseding Entra External ID (ADR-0023, build-order step 9 revised):** rather than provisioning Entra External ID as originally planned, Identity gained a second, parallel auth flow reusing its existing RS256 signing/JWKS/discovery machinery. New `Tenant` (this repo's first — previously `tenant_id` was a pure claim value with no owning record) and `OrganizerAccount` (email+password, `PasswordHasher<TUser>` hashing via `Microsoft.Extensions.Identity.Core` — not the full ASP.NET Core Identity stack, and not OTP's hand-rolled HMAC scheme) domain. `POST /v1/organizers/register` creates a `Tenant` + its first `OrganizerAccount` atomically (self-service signup; one organizer per tenant this pass, no invite-a-teammate flow yet); `POST /v1/organizers/login` verifies credentials, locking out after 5 failed attempts for 15 minutes (same attempt-capped pattern as OTP). `ITokenIssuer.IssueAsync` generalized to take `(subjectId, role, tenantId, ct)` — an organizer token carries `role: "organizer"` **and** `tenant_id` (unlike a buyer token, which omits it per ADR-0022). Frontend: dev-login retired from the UI entirely — `/admin/login` now renders a real `OrganizerAuthFlow` (login/register tabs); `LoginPage.tsx`/`authApi.ts` deleted (the gateway's `POST /api/auth/dev-login` endpoint itself is untouched, kept for curl/script testing); `UnauthorizedPage`'s "Log in" link is now route-aware (`/admin/login` vs `/login`).
  - ⬜ Invite-a-teammate / multi-organizer-per-tenant — explicitly deferred, not built
  - ⬜ Organizer password reset / forgot-password self-service — not built yet
- ✅ **Scan hardening, tour/leg date invariants, and entry gates (ADR-0024):** `POST /v1/tickets/scan` now requires an `eventId` (404s a ticket scanned at the wrong event, same shape as an unknown token) and rejects check-in outside the event's `DoorsOpenAt`→`EndsAt` window; Ticketing reads both live from Catalog via a new outbound Dapr client (`ICatalogEventClient`/`DaprCatalogEventClient`, its first outbound Dapr call — mirrors Inventory's `DaprSeatMapClient`). New `EntryGate` entity on Catalog (organizer-defined, per event); `Seat`/`GeneralAdmissionSection` carry an optional `EntryGateId` set once at `DefineSeatMap` time; scan resolves a ticket's gate restriction the same live way (a GA ticket's Catalog section id is resolved via Inventory's existing anonymous GA-allocations endpoint, `IInventoryGaClient`) and 409s a wrong-gate scan — an unscoped "any gate" scanner bypasses the restriction, a deliberate supervisor posture. `CreateEvent`/`UpdateEventDetails` now reject a leg whose dates fall outside its tour's own advertised range or overlap a sibling leg (`CreateEventCommand` changed from `IRequest<Guid>` to an outcome-carrying `IRequest<CreateEventResult>`, mirroring `DefineSeatMap`'s pattern), and `BookingEndsAt` can never be later than the leg's own `StartsAt`; also fixed an incidental gap where `CreateEvent` never checked a supplied `EventGroupId` belonged to the caller's own tenant. Frontend: `CreateEventPage`/`AdminEventDetailPage` mirror the date rules inline; new `EntryGatesPanel` (event detail page) manages gates and a `Select` per seat-map section assigns one; `ScanTicketPage` gained event + gate pickers.
- ✅ **Warm-once scan cache, real QR codes, and camera scanning (ADR-0025, supersedes ADR-0024's live-call decision):** triggered by the "millions of users waiting in line" throughput requirement, `ScanTicketAsync`'s two live per-scan Dapr calls (Catalog + Inventory) are gone — replaced with a one-time-per-event warm: a new Ticketing subscription to `EventPublished` (its second, after `OrderConfirmed`) runs `EventScanContextProvisioningService` (idempotent on redelivery, mirrors `InventoryProvisioningService`), persisting `EventScanContext` (check-in window, straight from the event payload — `EventPublished` gained `StartsAt`/`EndsAt`/`DoorsOpenAt`), `SeatEntryGate`, and `GaAllocationGate` rows into Ticketing's own Postgres (`ICatalogEventClient`/`IInventoryGaClient` reused as-is, just called once per event instead of once per scan; a GA-allocation race against Inventory's own async provisioning off the same event is covered by a bounded 5×2s retry). `ScanTicketAsync` now only ever reads these three local indexed tables — zero cross-service calls, zero dependency on Catalog/Inventory being reachable at scan time. Deliberately Postgres-backed, not Redis or in-memory-only (a fresh/scaled-out pod would have no way to "catch up" on a missed pub/sub delivery). First `HorizontalPodAutoscaler` in the repo (`deploy/base/ticketing/hpa.yaml`, CPU-based, 1–10 replicas as tunable starting defaults) plus a pre-scale-ahead-of-gate-open runbook note. Real QR codes: `GET /v1/tickets/{id}/qrcode` generates a PNG on demand (`QRCoder`, encoding the ticket's raw token, auth: buyer or owning tenant, opaque 404 on mismatch) — the buyer's order page fetches it as an authenticated blob (`responseType: 'blob'` + `URL.createObjectURL`, since a plain `<img src>` can't carry a bearer token) and renders it above the existing raw-token text. Camera-based scanning on `ScanTicketPage`: prefers the native `BarcodeDetector` Web API where available, falls back to `jsQR` elsewhere, feature-detected at runtime (new ambient `barcode-detector.d.ts` — not yet in TS's bundled DOM lib); a successful decode auto-submits through the same `handleScan` path the manual/hardware-wedge flow already uses. The manual/hardware-wedge token field stays first-class, not hidden — for true "millions in line" throughput, dedicated hardware laser/2D-imager turnstile scanners (already supported via that same input, zero further work) remain the realistic mechanism; the camera is the right fit for staff walking a line with a phone/tablet, not the scale fix itself (Part A — the cache — is what actually answers that).
- ✅ **Virtual waiting-room Queue service (ADR-0026, closes the P1 deferred item below):** a new, ninth backend service (`services/queue/`, port 5088) gating seat holds behind a paced admission queue for high-demand on-sales — opt-in per event via new `Event.RequiresQueue` (Catalog, the single on/off source of truth, propagated via `EventPublished` to both Inventory and Queue, same Draft-only-until-publish lifecycle as `OnSaleAt`/`BookingEndsAt`/`MaxTicketsPerBuyer`). `QueueSettingsProvisioningService` provisions per-event `QueueSettings` (Postgres, the only durable state — `AdmissionRatePerInterval`/`IntervalSeconds`/`SessionTtlSeconds`, tunable post-publish via the organizer's own `PUT /queue/settings`, unlike most Catalog-propagated settings, since pacing only matters once an event is live). The waiting-room state itself is deliberately Redis-only, not Postgres — a FIFO sorted set per event plus TTL'd admission markers (`RedisQueueStore`, mirroring `RedisHoldStore`'s Lua-script pattern), a genuine asymmetry with ADR-0025's durable scan cache since a lost queue position is just "back of the line," not a broken check-in. `QueueAdmissionController` (a `BackgroundService` mirroring `ExpiredHoldReaper`) promotes waiting sessions via `ZPOPMIN` (atomic, so concurrent replicas can never double-promote) and mints an HMAC-signed admission token per session. `HoldService.PlaceHoldAsync` gains one more settings-derived check (`PlaceHoldOutcome.QueueAdmissionRequired`) verifying that token **locally** (`HmacQueueAdmissionTokenValidator`, shared secret `QueueAdmission:HmacKey`) — zero call back to Queue at hold time, the same "propagate once, verify locally" philosophy ADR-0025 established for Ticketing's scan. Frontend: a new anonymous `QueueWaitingRoomPage` (join/poll, no max-attempts cap unlike ticket polling, sessionStorage-backed session id so a refresh resumes position) that `EventDetailPage`'s "Select seats" now routes through when `requiresQueue` is set, and `SeatSelectionPage` defends against a direct URL hit the same way.
  - ⬜ Replay/one-time-use enforcement on an admission token — not built; a hold's own capacity/limit checks are the accepted backstop this pass
- ✅ **Ticket cancellation & refund workflow (closes the P5 deferred item below):** buyer-initiated `POST /v1/orders/{id}/cancel` (Ordering, gateway-routed), a new `CancelOrderWorkflow` Dapr Workflow saga mirroring `CheckoutWorkflow`'s shape but with a deliberately different step order — void tickets → release sold inventory → refund → mark refunded (unwinding a *completed* sale takes back the product before returning the money, the opposite of `CheckoutWorkflow`'s compensation order, which unwinds a checkout that never finished). Inventory gained `HoldService.CancelSoldAsync` (`POST /v1/holds/{holdId}/cancel`, internal/saga-only) — the reverse of `ConvertToSoldAsync`, releasing sold seats/GA quantities back to available with the same single-status-gate idempotency (`HoldStatus.Cancelled`), extending `GeneralAdmissionAllocation`/`InventoryItem` with `ReleaseSold`. Ticketing gained `TicketVoidingService.VoidByOrderAsync` (`POST /v1/orders/{orderId}/tickets/void`, internal) — all-or-nothing across an order's tickets, mirroring `SeatBlockingService`'s precedent (any already-checked-in ticket blocks voiding the whole order). Payments needed **zero** changes — `RefundActivity`/`IPaymentClient.RefundAsync`/`POST /v1/payments/refund` were already fully built and unused outside saga compensation; this wires them into a real buyer-facing path for the first time. Frontend: `OrderPage` gets a "Cancel order" button (visible only while `Confirmed`) behind an Ant `Modal.confirm`, mapping `NotConfirmed`/`TicketAlreadyCheckedIn`/`Failed` to distinct messages.
  - ⬜ A dedicated Redis instance for Queue in production (dev shares Inventory's) — flagged, not built
  - ⬜ Bot/automation defense at the queue-join step (rate-limiting, CAPTCHA) — not built, a real hardening gap for a genuine high-demand on-sale
  - ⬜ Randomized queue-position ordering — current implementation is strict FIFO only; the enterprise requirements doc asks for an option to randomize positions at queue-open (e.g. to avoid rewarding whoever scripts the fastest join), not built
  - ⬜ Abandoned-waiting-session expiry — `SessionTtlSeconds` only expires an *admitted* session's token; a session that joins the waiting sorted set and never returns has no TTL at all today and sits in Redis indefinitely, unlike Inventory's `Hold`, which has `ExpiredHoldReaper` for exactly this kind of abandonment
- ✅ **Manual sales pause/resume (ADR-0027, closes the P6 deferred item below):** a new `Event.SalesPaused` bool (Catalog) lets an organizer halt/resume new holds for an already-`Published` event on demand — independent of the static `OnSaleAt`/`BookingEndsAt` window, and without touching already-placed holds/tickets. `PauseSales`/`ResumeSales` vertical slices (`POST /v1/events/{id}/pause-sales`/`resume-sales`, same tenant-ownership/409-conflict shape as `PublishEvent`) publish new `EventSalesPaused`/`EventSalesResumed` integration events — not a redelivered `EventPublished` (dead path — `InventoryProvisioningService.ProvisionAsync`'s idempotency guard would just short-circuit) or the zero-consumer `EventUpdated`. Inventory's new `EventSalesToggleService` consumes both via two new Dapr subscriptions, updating `EventInventorySettings.SalesPaused` directly; `HoldService.PlaceHoldAsync` checks it first, before the on-sale/booking-cutoff checks (`PlaceHoldOutcome.SalesPaused`, 409). Frontend: a "Pause sales"/"Resume sales" toggle on `AdminEventDetailPage` (rendered only for `Published` events), buyer-facing gating in `EventDetailPage`/`SeatSelectionPage` mirroring the existing `onSaleAt` pattern exactly.
  - ⬜ Gate/doors-open time and price changes post-publish remain out of scope — both need real propagation redesign (Ticketing's scan-window cache and Inventory's baked-in seat/GA price are both populated once at publish and never re-read), see ADR-0027's Alternatives.
- ✅ **Frontend: inline tour creation + upcoming/past legs on `CreateEventPage`:** organizer feedback that creating a tour was a disconnected, separate-page flow from creating its first leg. `EventGroupPicker` dropped its instant-create modal (which called `POST /v1/event-groups` the moment "+ New tour" was picked, immediately, even if the form was later abandoned) in favor of a plain `Select`; `CreateEventPage` now renders either an inline "New tour title" field (deferring tour creation to the event's own submit — no orphan tour if abandoned, and the new tour inherits this first leg's own dates as its advertised range) or, when an existing tour is picked, a new `TourLegsList` (upcoming legs always visible, past ones collapsed behind a count toggle) so the organizer sees what's already scheduled before adding another. `TourLegsList` is reused on `AdminEventDetailPage` (a "Part of a tour" card, once an event has one) and `AdminEventsPage` gained a "Tour" column (client-side batched `getEventGroup` lookups over the current page's distinct ids — Catalog has no batch endpoint for this). `CreateEventGroupPage` (`/admin/tours/new`) is unchanged, still the only way to set a tour's full details (contact/social, wider date range) after creation.
  - ⬜ Still no UI to edit an already-created tour's contact/social (dates gained a small "Edit dates" action on `TourDetailPage` in the multi-leg-repeater pass below) — this pass only touched creation-time and read-only display flows.
- ✅ **Frontend: graceful load-failure states, no more stacked error toasts:** a failed data fetch (backend down, network error) was showing up to 4 stacked toasts on one page load — the axios interceptor's own generic 5xx/network toast plus the page's own `toast.error('Could not load X.')`, doubled again by React StrictMode's dev-mode double-invoke — while the page's "No events found" empty state rendered underneath anyway, making a real failure indistinguishable from a genuine empty result. `httpClient`'s interceptor no longer auto-toasts at all (401 session-clear is the only status it still handles globally) — every call site, load or action, already had its own specific handler. New `LoadError` (inline Ant `Result`, "Couldn't load this" + retry) replaces the toast on every list/panel's own failed load (`EventsListPage`, `AdminEventsPage`, `EventGroupsPage`, `MyOrdersPage`, `AdminOrdersPage`, `SeatBlockPanel`, `TourLegsList`), tracked as its own `loadError` state distinct from "succeeded with zero results." Single-resource detail pages (`EventDetailPage`, `AdminEventDetailPage`, `OrderPage`) now check `error.response?.status === 404` to show `NotFoundPage` only for a genuine 404, `ServerErrorPage` (previously reserved for route-level render errors only) for anything else — previously *any* failure on those pages, including a network error, showed the misleading "not found" message. Action-triggered toasts (save, publish, block/unblock, login) are unchanged.
- ✅ **Frontend: tour-first flow — `TourDetailPage` + "Add leg" (superseded below):** the inline quick-create above was still event-first; organizer feedback wanted tour-first instead — create the tour, land on its own page, keep adding legs from there. New `TourDetailPage` (`/admin/tours/:id` — dates/contact summary + `TourLegsList`) gets an "Add leg" button linking to `CreateEventPage?eventGroupId=<id>`, which now reads that query param via `useSearchParams` to pre-fill the tour picker. `CreateEventGroupPage` now lands on the new tour's own page instead of the list; `EventGroupsPage`'s rows are clickable through to the same page. The inline "+ New tour" fast path on `CreateEventPage` itself is unchanged and still available for the "already creating an event, realize it needs a tour" case. **This one-leg-at-a-time-via-navigation shape turned out not to match the actual ask** — see the next bullet.
- ✅ **Frontend: single-page multi-city leg repeater on `CreateEventPage` (replaces the per-leg-navigation flow above):** the tour-first flow still made adding several legs a repeated navigate-away-and-back cycle; the actual requirement was one page/form that defaults to a plain single-event form and grows inline into a multi-leg batch via an "Add" action, all submitted together in one visit — e.g. "Event X plays 4 cities next week" becomes 4 leg cards on one page, not 4 separate page visits. New `EventLegFields` (`Form.List name="legs"`, one `Card` per city/date, mirrors `SeatMapSectionsFields.tsx`'s repeater shape exactly) replaces `CreateEventPage`'s single set of event fields; "Add another city/date" appends another card and, once there's more than one leg, the tour picker becomes required (auto-switching to "+ New tour" if nothing was picked yet — a multi-leg batch always needs somewhere to attach). Submission creates the tour first if needed, then calls `POST /v1/events` **sequentially** (never in parallel — the server's sibling-overlap check re-queries fresh state per call) and backfills a newly-created tour's date range from `min`/`max` across the submitted legs afterward. **No rollback exists for a partial failure** (no delete endpoint for `Event`/`EventGroup`), so each leg's status (`pending`/`created`/`failed`) is tracked and shown inline — already-created legs lock and tag themselves, the submit button relabels to "Create remaining legs," and a retry only (re-)submits what's left. `TourDetailPage`'s "Add leg" and `CreateEventGroupPage` are unchanged in purpose (still the way to set a tour's full metadata up front, or add exactly one more leg later) — both now funnel into this same repeater-capable page rather than a single-leg-only form. `TourDetailPage` also gained a small "Edit dates" action (`Modal` + `updateEventGroup`) — closes a real, separately-noted gap (there was previously no way to edit a tour's dates at all) surfaced while wiring this pass's own date-range-backfill fallback message.
- ✅ **EF Core migrations everywhere, and the database-provider seam (ADR-0029):** `EnsureCreatedAsync()` is gone from every service. All eight database-owning services ship committed `Migrations/`, applied by running the *same image* with `--migrate` — locally via `scripts/db-migrate.sh` (called by `dev-up.sh` before anything starts serving) and in-cluster as an Argo CD **PreSync Job** per service, so a failed migration fails the sync with inspectable logs instead of crash-looping a replica. Services never migrate themselves: startup migration races itself across replicas and takes the app down on a bad migration. The migrate Job carries no `dapr.io/*` annotations on purpose (a Dapr sidecar never exits, so the Job would never complete) and reuses the Deployment's image placeholder byte-for-byte, so CI's `kustomize edit set image` rewrites both to the same tag — the schema is never applied by a different build than the one about to serve traffic. CI carries a migration-drift guard. `.editorconfig` exempts `Migrations/` from all analyzers via a `**.cs` glob (**not** `**/*.cs`, which requires an intermediate directory and so never matched the files EF actually writes — that section had been silently inert since it was written).
- ✅ **Cluster ingress and TLS (ADR-0030):** the cluster gained a single real HTTPS entry point. Terraform (`infra/environments/dev/ingress.tf`) installs ingress-nginx + cert-manager and a Let's Encrypt `ClusterIssuer` — like Argo CD and the Dapr control plane, these can't be installed *by* the thing they're a prerequisite for, so they're tracked IaC rather than `kubectl apply` by hand. The hostname is Azure's free `<label>.<region>.cloudapp.azure.com` (via `service.beta.kubernetes.io/azure-dns-label-name`), which Let's Encrypt will issue for because `cloudapp.azure.com` is on the Public Suffix List — no domain purchase needed to get a real certificate. The gateway's Service went `LoadBalancer` → **`ClusterIP`**: leaving it a `LoadBalancer` would mint a second public IP serving plain HTTP with no certificate, bypassing the ingress entirely. `externalTrafficPolicy: Local` is load-bearing, not cosmetic — Queue's join rate limiter buckets by client address, and the default policy SNATs every caller to one node address. The Ingress deliberately knows nothing about individual services, so the gateway's own route allowlist stays the only way in.
- ✅ **OpenTelemetry Collector and a real trace backend (ADR-0031):** every service has always exported OTLP; until now nothing in the cluster listened, so traces went nowhere. `deploy/base/observability/` runs an OpenTelemetry Collector (contrib distro, for the `azuremonitor` exporter) exporting to a workspace-based Application Insights resource that the `log-analytics` Terraform module now owns alongside the workspace — one workspace, one retention setting, one daily cap covering both. Services reach it via `OTEL_EXPORTER_OTLP_ENDPOINT` in the shared config map; **Dapr sidecars** reach it via a `Configuration` CRD referenced by each pod's `dapr.io/config` annotation — dropping that half would still give connected traces (Dapr propagates `traceparent` regardless) but every pub/sub delivery would be an unexplained gap in the timeline. **No logs pipeline, on purpose:** the services write to stdout and Container Insights already ships those lines to the same workspace, so a second path would bill the same lines twice against one daily cap. The pipeline config is a `configMapGenerator`, not a hand-written ConfigMap, so editing it changes the generated name and actually rolls the collector.
- ✅ **Real token validation per environment (ADR-0032) — found by a completeness audit, not by a failing test:** Identity signs RS256 with issuer `https://identity.eventplatform.example`, but all eight deployed services were taking the HS256 `Jwt:DevSigningKey` branch, so **no service could validate an Identity-issued token**. Because dev-login had already been retired from the UI, a deployed environment would have let a user log in and then 401 on everything — and it was invisible at deploy time, since every pod comes up green either way. Fixed by removing the dev-key env block from all eight Deployments and six migrate-Jobs and setting `Jwt__Authority=http://identity` + `Jwt__RequireHttpsMetadata=false` in the overlay's config map, with `Identity__Jwt__Issuer` on the Identity Deployment pinned byte-identical to that Authority (validation compares the token's `iss` to the issuer in the discovery document, and a mismatch rejects every token with an error naming neither URL). The same audit found **Queue** had no dev key at all and was already on the `Authority` branch pointed at a nonexistent placeholder domain — its tenant-owned settings endpoints were simply broken — and that **Communication** was silently resolving every channel to the `dev-log` sender in-cluster, now a loud startup warning outside Development. Local dev moved to the same RS256 path (`Jwt:Authority=http://localhost:5087`), so there is no longer a local-vs-deployed auth divergence to be surprised by; the HS256 branch and the synced `jwt-dev-signing-key` survive as a deliberate, unused escape hatch.
- ✅ **SPA deployment (ADR-0033):** the HTTPS hostname served an API and nothing a person could open — the React app wasn't deployed at all. It now ships as a two-stage build (`node:22-alpine` → `nginx:1.27-alpine`, port 8080, no Node or source in the runtime image). The ingress splits on path — `/api` to the gateway (longer prefix wins), everything else to the SPA, on **one hostname** — which is what makes `VITE_GATEWAY_BASE_URL` safe to leave unset: axios issues same-origin relative requests, so there is no CORS for the deployed app and **one image serves every environment** (Vite inlines `VITE_*` at build time, so a baked-in hostname would mean one build per environment, which is exactly what makes "promote the tested artifact" impossible). The only build-time value that survives is the Stripe *publishable* key, passed as a GitHub Actions **variable** rather than a secret to say plainly that it's public by design. nginx carries two non-obvious rules: `try_files … /index.html` (the router is client-side, so refreshing any inner page would otherwise 404) and `no-cache` on `index.html` while `/assets/` is `immutable` for a year (Vite fingerprints asset filenames, but `index.html` is the only file naming the current hashes — cache it and a returning visitor asks for assets the new deploy already deleted). Consequence worth knowing: the gateway's `/health/*` and `/scalar/v1` are no longer reachable from outside, since `/` now goes to the SPA.
- ✅ **CI now checks infrastructure, not just code:** a new `infrastructure` job runs `terraform fmt -check` + `terraform validate` on both root modules (with `-backend=false`, since the real backend needs Azure reachability CI doesn't have) and `kustomize build deploy/overlays/dev`, which exercises the base, every patch and every generator — a patch that targets nothing, or a resource missing from a `kustomization.yaml`, now fails there rather than at Argo CD sync time. Added specifically to close a repeated "unverified beyond formatting" caveat, and it earned its place immediately: its first run caught a deprecated `commonLabels` usage, and the `build` job caught a real compile error in the same window. It proves the manifests are well-formed, not that they do what was meant — it talks to no cluster.
- ✅ **One-command Azure provisioning (`scripts/provision-azure.sh`):** replaces a multi-step README dance whose steps had to be run in the right order with values copied between them by hand — the source of two real incidents (Terraform state created in whichever subscription the CLI happened to default to, and backend-config values transcribed wrongly). The script logs in, **always asks which subscription** rather than inheriting one silently, bootstraps remote state if needed, fills any missing required tfvars value, then plans and applies only behind a typed confirmation. It is safe to re-run: existing bootstrap state, tfvars and init are detected and reused. Two failure modes it specifically defends against, both hit for real while building it: local bootstrap state that names resources living in a *different* subscription (verified against Azure, not just checked for file existence, since trusting the file turns this into a confusing `terraform init` 404 several steps later), and a stale `subscription_id` left in tfvars — which Terraform reads in preference to the CLI's selected context, so leaving it alone would deploy into the wrong subscription with no warning. `infra/bootstrap` gained a **required** `subscription_id` for the same reason.

---

## ⏸️ Deferred — CLOUD tasks (ON HOLD — do not lose)

**No longer paused** — cloud work resumed and most of this table has shipped for
the `dev` topology (ADR-0017). Everything below is written against real, applied
code in `infra/`, `deploy/` and `.github/workflows/`; the ⬜ rows are what is
genuinely still missing. Nothing here has been billed against a real
subscription yet — `terraform apply` is the user's own step.

| # | Task | Status | Related |
|---|------|--------|---------|
| C1 | GitHub Actions CI (`dotnet build`/`test`, analyzers, coverage) | ✅ `ci.yml` — `build` (solution build + `dotnet test`), `frontend` (lint/typecheck/build), `infrastructure` (`terraform fmt -check`/`validate` on both roots, `kustomize build` on the dev overlay). No coverage gate yet | ADR-0004 |
| C2 | Terraform: AKS cluster + node pools (system/general/hot-path/spot) | ✅ for dev — single system pool, Free control plane, CNI Overlay, OIDC + Workload Identity on. The dedicated hot-path/spot pools remain the **production** target, deliberately not built for dev | ADR-0002/0005, ADR-0017 |
| C3 | Argo CD / GitOps + `deploy/` manifests | ✅ Argo CD installed by Terraform (`argocd.tf` — it can't deploy itself); Kustomize base + dev overlay reconciled from `deploy/` | ADR-0004 |
| C4 | Dockerfiles + Helm charts per service | ✅ Dockerfiles (one shared parameterised root `Dockerfile` + `frontend/Dockerfile`). **Kustomize, not Helm** — a deliberate substitution, not an omission | Phase 0 |
| C5 | Azure infra: PostgreSQL, Redis, Service Bus/Event Hubs, Key Vault | ✅ Postgres Flexible Server (8 databases), Azure Cache for Redis, Key Vault (RBAC mode), ACR, blob storage, Log Analytics + App Insights. **Service Bus deliberately not built** — dev uses Redis for pub/sub (ADR-0017); Service Bus stays the production target | ADR-0001, ADR-0017 |
| C6 | Point every service's `Jwt:Authority` at Identity's real deployed URL | ✅ done (ADR-0032) — the dev overlay sets `Jwt__Authority=http://identity`; no service ships a dev signing key any more | Security, ADR-0023/0032 |
| C7 | Cloud Dapr components (Service Bus / Azure Cache / Key Vault) — mirror local | ✅ Dapr control plane installed by Terraform (`dapr.tf`); `deploy/base/dapr-components/` carries `pubsub`/`statestore` under the **same names** as the local files, both Redis-backed against Azure Cache | ADR-0006, ADR-0017 |
| C8 | Azure Front Door + WAF + bot management (edge) | ⬜ not built — ingress-nginx + cert-manager + Let's Encrypt on Azure's free `*.cloudapp.azure.com` FQDN serve this role for dev (ADR-0030). Front Door/WAF remains the production target | Phase 3, ADR-0030 |
| C9 | Observability backend (Azure Monitor / managed Grafana) | ✅ workspace-based Application Insights + an in-cluster OpenTelemetry Collector (traces + metrics; no logs pipeline on purpose — Container Insights already ships stdout to the same workspace). Managed Grafana not built | Observability, ADR-0031 |
| C10 | Multi-AZ + DR runbook (RTO < 15m / RPO < 1m) | ⬜ not built — the dev topology is deliberately single-AZ, single-node (ADR-0017) | Phase 3 |
| C11 | Multi-region / geographic distribution — beyond C10's single-region Multi-AZ scope | ⬜ not built | Enterprise requirements doc |
| C12 | Backup/restore automation + a tested restore procedure (distinct from C10's DR-runbook-as-a-document) | ⬜ not built | Enterprise requirements doc |
| C13 | One-command provisioning (`scripts/provision-azure.sh`) — login, subscription choice, bootstrap-if-needed, tfvars, plan, typed-confirmation apply | ✅ built and re-runnable; never applies unprompted. Not yet exercised against a real subscription end-to-end | ADR-0017 |

## ⏸️ Deferred — GitHub / ops actions (need repo owner)

| # | Action | Notes |
|---|--------|-------|
| O1 | Make repo **private** | currently public |
| O2 | Branch protection on `main` (PR + review + linked issue + checks) | enforces golden rules |
| O3 | Add `ADD_TO_PROJECT_PAT` secret | for project auto-add workflow |
| O4 | Move auto-add workflow to `main` | issue-triggered workflows run from default branch |
| O5 | Invite teammates to repo + project | |
| O6 | ~~Remove old `services/catalog/src/`~~ — done | leftover from the structure flatten; it still carried an `EnsureCreated` that contradicted ADR-0029 |

---

## ⏸️ Deferred — Product features

| # | Feature | Notes |
|---|---------|-------|
| P1 | **Virtual waiting room / queue system** for high-demand on-sales (Ticketmaster/AXS/Queue-it-style) | **Done** — see the ✅ Virtual waiting-room Queue service bullet above (ADR-0026). |
| P2 | **Buyer flow: browse/select fully anonymous, OTP-gate only at "hold seats," OTP verification *is* login** | Refines how the now-built Identity service (phone+OTP, see the ✅ bullet above) should be wired into the buyer frontend — the backend half (`POST /v1/otp/request`/`verify` issuing a real JWT, `HoldService.PlaceHoldAsync` already taking a `userId`) is done; this entry is the still-pending **frontend** UX decision. Decision: `EventsListPage`/`EventDetailPage` stay anonymous (already true today); `SeatSelectionPage` (currently behind `ProtectedRoute`) should *also* become reachable without a prior login — a buyer picks seats/GA quantities freely. The identity gate moves to the **hold action** itself (clicking "Hold selection"), not a separate upfront login wall and not deferred all the way to checkout: placing a hold claims real, scarce inventory, so it's the natural point to require a lightweight phone+OTP check anyway — this doubles as abuse prevention against anonymous hold-spam/seat-hoarding, which a no-login flow would otherwise leave wide open. A successful OTP verification mints the session/JWT on the spot (that *is* the login, no separate account-creation step); that identity then owns the hold through checkout and ticket issuance. "Complete your profile" (name, email, etc.) becomes an optional post-purchase prompt, never a blocker. Frontend implication: move the `ProtectedRoute` wrapper off `/events/:id/seats` and trigger OTP verification from the "Hold selection" button instead (e.g. an inline modal), not a route-level redirect to `/login` — this is the same work tracked as build-order step 10 (`AuthContext` swap). **Done** — see the ✅ Identity/frontend bullets above. |
| P3 | **Archive/purge consumed tickets** — design only, nothing built (ADR-0021) | Once a ticket is scanned (checked in), its hot-table row is rarely needed again, but purging it immediately is unsafe (same-day disputes/re-entry). Phased design: **Phase 1** a same-DB `ticket_archive` table plus an organizer-triggered "archive this event's tickets" action (copy then delete from the hot `ticket` table) — the trigger is time-based-by-event, **not** per-ticket-at-scan-time. **Phase 2** an automatic sweep via a `BackgroundService` (`PeriodicTimer` + scoped `IServiceScopeFactory` + try/catch-continue, the exact shape `ExpiredHoldReaper` already establishes), triggered by a retention window past `Event.EndsAt` — needs `EndsAt` denormalized onto `Ticket` at issuance to avoid N cross-service calls to Catalog per sweep. **Phase 3** swaps the archive table for real blob/cold storage once storage cost (not just live-table size) justifies it — the same port-plus-swappable-adapter pattern already used for `IPaymentGateway`/`IEmailSender`, so it's a drop-in adapter later, not a rewrite. Not started. |
| P4 | **Promo codes / discounts / offers** | No pricing-adjustment mechanism anywhere — `OrderLine.UnitPriceMinor` always comes straight from Catalog's `PriceAmount`. Needs a discount entity (Catalog or a new pricing concern), validation at checkout, and threading through `CreateOrderActivity`/`OrderConfirmed`. Not started. |
| P5 | **Ticket cancellation & refund workflow (buyer-initiated)** | **Done** — see the ✅ Ticket cancellation & refund workflow bullet above. |
| P6 | **Admin pause/resume sales** | **Done** — see the ✅ Manual sales pause/resume bullet above (ADR-0027). |
| P7 | **Admin: issue complimentary tickets** | No path for an organizer to mint a ticket outside the normal hold→checkout→confirm flow (e.g. press comps, giveaways). Not started. |
| P8 | **Admin: manage users & permissions** | No RBAC beyond the unenforced `role` claim (see the Security & abuse-prevention table below) and no multi-organizer-per-tenant/invite flow (flagged as out of scope when organizer auth shipped, ADR-0023). Not started. |
| P9 | **Shopping cart across multiple events** | Checkout today is single-hold-at-a-time (`POST /v1/checkout` takes one `holdId`) — no way to buy tickets to several different events, or bundle multiple legs of a tour (ADR-0019's explicitly deferred "complete package" purchase), in one order. Not started. |

---

## ⏸️ Deferred — Security & abuse prevention

| # | Item | Notes |
|---|------|-------|
| S1 | Bot/CAPTCHA defense at signup, OTP-request, and queue-join endpoints | `RequestOtpHandler` has a per-phone resend cooldown but no CAPTCHA/bot-scoring; the queue's join endpoint has no rate-limiting either (see the Queue bullet's own ⬜ sub-items above) |
| S2 | General API rate limiting | No rate-limiting middleware anywhere in the gateway or any service today |
| S3 | Fraud detection | No mechanism anywhere — payment/booking-pattern anomaly detection is entirely unbuilt |
| S4 | Real RBAC enforcement | The `role` claim (`buyer`/`organizer`) is UI-routing only — `ProtectedRoute`/`AdminLayout` check "is anyone logged in," never "is this the right role" or "does this organizer have this permission." Explicitly documented as not a security boundary in `frontend/CLAUDE.md` |
| S5 | Hardened token storage (httpOnly/Secure/SameSite cookie + CSRF) | Currently sessionStorage-held bearer tokens for both buyer and organizer sessions — flagged as required before a real production rollout since ADR-0015, still not built |
| S6 | Unified security/audit log (login attempts, admin actions) | Distinct from the per-service domain ledgers that already exist (Inventory's `LedgerEntry`, Payments' `ProcessedWebhookEvent`) — no cross-cutting "who did what, when" trail for auth events or admin actions today |

---

## ⏸️ Deferred — Reporting & analytics

| # | Item | Notes |
|---|------|-------|
| R1 | Sales/revenue reporting | No reporting endpoint or read model anywhere — organizers can only see raw order/ticket lists, no aggregation |
| R2 | Event performance reporting | No per-event dashboard (sell-through rate, sales velocity, etc.) |
| R3 | Queue statistics & peak-traffic reporting | Queue service has no admin-facing view of queue depth/throughput over time — only the buyer-facing live position/ETA is built (flagged as a follow-up in the Queue bullet's own scope-cut list above) |
| R4 | Booking trends | No time-series/cohort view of booking activity |
| R5 | Payment success/failure rate reporting | Payments logs every webhook event (`ProcessedWebhookEvent`) but exposes no aggregated success/failure-rate view |
| R6 | User analytics | No buyer-behavior analytics (funnel drop-off, repeat-purchase rate, etc.) |
| R7 | Business-metrics pipeline + dashboards | Distinct from/complementary to C9's infra-focused observability backend (Azure Monitor/Grafana) — this is business-metric aggregation, not infrastructure telemetry |

---

## Tech debt / to verify

| # | Item | Notes |
|---|------|-------|
| T1 | Confirm CPM package versions | ✅ build green — versions resolve, vulns patched |
| T2 | Build the scaffold locally (`dotnet build`) | ✅ green |
| T3 | MediatR pinned to **12.5.0** | do NOT let Dependabot bump to 13.x (commercial) — ADR-0014 |
| T4 | Dev HTTPS cert | `dotnet dev-certs https --trust` |
| T5 | Architecture-tests project (NetArchTest) | enforce ADR-0008/0009 boundaries in CI |
| T6 | Coverage gate + integration tests (Testcontainers) | Phase 1 exit criteria |
| T7 | Replace Catalog placeholder endpoint | ✅ done — real CreateEvent/GetEvent slices |
| T8 | Catalog EF Core migrations | ✅ done — `InitialCreate` committed under `Catalog.Infrastructure/Migrations/`. Superseded by T13, which covers every service |
| T9 | Transactional outbox building-block | ✅ `EventPlatform.Messaging` — write path (`IEventPublisher`/`OutboxMessage`/`ApplyOutbox`) + `OutboxRelay` (Dapr pub/sub, at-least-once). Consumed side (dedupe on CloudEvent id) lands with Inventory (#7). Needs the `outbox` table in migrations (T8) |
| T10 | Seat-map read path returns all seats inline | fine at Phase-1 dev scale; page/stream `GET /seatmap` (and the Inventory hand-off) before large-venue maps (50k+ seats). `PublishEvent` also loads seats just to count — swap for a `COUNT(*)` |
| T11 | Money as minor units assumes 2-decimal currency | Inventory `ToMinor` does `amount × 100`; refine per ISO 4217 exponent (JPY = 0, etc.) before multi-currency |
| T12 | Order currency is defaulted to `USD` | `CheckoutOptions.DefaultCurrency`; derive from the Catalog event instead (Order → Catalog client) |
| T13 | Real EF Core migrations across all services, not just Catalog | ✅ done — all eight database-owning services ship committed `Migrations/`. `EnsureCreatedAsync()` is gone: services never migrate themselves (ADR-0029); the same image run with `--migrate` applies the schema, locally via `scripts/db-migrate.sh` and in-cluster as an Argo CD PreSync Job. CI carries a migration-drift guard |
| T14 | Automated test projects for every service | ✅ done — all nine services have a `tests/` project. **Remaining is depth, not existence:** coverage is uneven (Inventory's no-oversell path is exercised against a real Redis via Testcontainers; Catalog/Ticketing are thinner), and there is still no end-to-end saga test against a real Dapr sidecar |

---

## How this is maintained

- Update on every meaningful change (part of "done").
- Cloud work has resumed — the cloud table now carries a per-row **Status**. Keep
  it that way: mark a row ✅ with what actually shipped, or ⬜ with why it didn't.
  Don't quietly delete a row that turned out to be replaced by something else
  (Front Door → ingress-nginx, Helm → Kustomize, Service Bus → Redis) — record the
  substitution, so the production target stays visible next to the dev reality.
- When board work resumes, mirror open items as GitHub issues.
